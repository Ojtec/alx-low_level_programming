#include "main.h"

/**
 * main - function to print lowerase
 *
 * Return: Output(okay)
 */
void print_alphabet_x10(void)
{
	char c, i;

	for (i = 0; i <= 0; i++)
	{
		for(c = 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
		_putchar('\n');
	}
}

